# LOF基金实时估值跟踪系统

## 📊 功能概述

这是一个专业的LOF基金实时估值和套利分析工具，针对**华宝油气(162411)**等QDII-LOF基金设计。

### 核心功能

1. **实时估值**: 基于基金净值估算和场内价格，计算实时溢价率
2. **套利分析**: 自动识别溢价/折价套利机会
3. **XOP跟踪**: 监控美股XOP行情，提供对冲参考
4. **数据记录**: 自动保存历史数据，便于回溯分析

## 🚀 快速开始

### 安装依赖

```bash
pip install requests
```

### 基础使用

```bash
# 单次查询
python3 lof_tracker.py

# 查询其他基金
python3 lof_tracker.py --code 161130  # 纳指LOF

# 持续监控
python3 lof_tracker.py --watch --interval 60

# 溢价超过1%时提醒
python3 lof_tracker.py --watch --notify 1
```

## 📈 数据解读

### 示例输出

```
=================================================================
📊 华宝油气(162411) 实时估值报告
=================================================================
⏰ 2026-03-26T11:56:11

【基金净值】
  最新净值: 0.965 (2026-03-24)
  估算净值: 0.9706 (0.58%)   ← 基金公司实时估算
  估算时间: 2026-03-26 04:00

【场内交易】
  当前价格: 0.984            ← 交易所实时价格
  开盘价: 0.969
  最高价: 0.985
  成交量: 7,530,080

【套利分析】
  场内价格: 0.984
  估算净值: 0.9706
  溢价率: +1.38%             ← 关键指标！

  🎯 套利机会: HEDGE
  📝 操作建议: 申购+XOP做空
  💰 预期收益: 1.23%
  ⚠️  风险提示: XOP基差风险
```

### 关键指标说明

| 指标 | 说明 | 套利阈值 |
|------|------|---------|
| **溢价率** | (场内价格-净值)/净值 | >1.5%: 溢价套利<br>>0.15%: XOP对冲<br><-0.5%: 折价套利 |
| **估算净值** | 基金公司实时估算(T+1) | 基于持仓股票实时计算 |
| **XOP涨跌幅** | 美股油气ETF涨跌 | 用于对冲套利风险 |

## 💰 套利策略详解

### 策略一：溢价套利（申购-卖出）

**条件**: 溢价率 > 1.5%

**操作**:
1. T日: 场内申购华宝油气
2. T+2日: 份额到账，场内卖出

**成本**: ~1.65% (申购0.15% + 卖出0.03% + 冲击成本)

**风险**: T+2期间净值下跌、溢价收敛

### 策略二：XOP对冲套利（推荐）

**条件**: 溢价率 > 0.15%

**操作**:
1. T日: 场内申购华宝油气 + 做空XOP（美股账户）
2. T+2日: 卖出华宝油气 + 平仓XOP

**成本**: ~0.15% (仅申购费)

**优势**: 几乎无风险，锁定溢价收益

**比例**: 1股XOP ≈ 根据实时仓位调整

### 策略三：折价套利（买入-赎回）

**条件**: 折价率 > 0.5%

**操作**:
1. T日: 场内买入华宝油气
2. T+1日: 场内赎回

**成本**: ~0.53% (买入0.03% + 赎回0.5%)

**资金占用**: T+4到账

## ⚙️ 高级配置

### 定时任务

```bash
# 编辑crontab
crontab -e

# 每5分钟检查一次，记录到日志
*/5 9-15 * * 1-5 cd /path/to/fund_arbitrage && python3 lof_tracker.py >> logs/tracker.log 2>&1

# 每天收盘后汇总分析
15:05 * * 1-5 cd /path/to/fund_arbitrage && python3 analyze_daily.py
```

### 多基金监控

```python
# monitor_all.py
funds = ["162411", "161130", "161128", "164824"]

for code in funds:
    tracker = LOFTracker(fund_code=code)
    result = tracker.track()
    if result.get("premium", {}).get("opportunity"):
        send_alert(result)  # 发送通知
```

### 数据存储

跟踪数据自动保存到 `tracking_data/` 目录：
- 文件名: `{基金代码}_{日期}.jsonl`
- 格式: 每行一个JSON对象，包含完整跟踪数据

## 📊 历史数据分析

### 查看历史溢价分布

```python
import pandas as pd
import json

# 读取历史数据
data = []
with open("tracking_data/162411_20260326.jsonl") as f:
    for line in f:
        data.append(json.loads(line))

df = pd.DataFrame(data)
df['premium'] = df['premium'].apply(lambda x: x['premium'])

# 统计
print(f"平均溢价: {df['premium'].mean():.2f}%")
print(f"最大溢价: {df['premium'].max():.2f}%")
print(f"溢价>1%次数: {(df['premium'] > 1).sum()}")
```

## 🔧 数据源说明

| 数据 | 来源 | 更新频率 |
|------|------|---------|
| 基金净值/估算 | 天天基金网 | 实时(估算) |
| 场内价格 | 腾讯财经 | 实时(3秒) |
| XOP行情 | 新浪财经 | 延时15分钟 |
| 汇率 | 新浪财经 | 实时 |

## ⚠️ 风险提示

1. **估算误差**: 基金公司估算净值与实际净值可能有偏差
2. **汇率风险**: QDII基金受汇率波动影响
3. **流动性风险**: 大额交易可能影响市场价格
4. **XOP基差**: XOP交易价格与净值可能有差异
5. **政策风险**: QDII额度限制可能导致暂停申购

## 📚 参考资料

- Palmmicro估值工具: https://palmmicro.com/woody/res/sz162411cn.php
- 华宝油气官网: http://www.fsfund.com
- XOP详情: https://www.sectorspdr.com/sectorspdr/sectors/xop

## 🎯 套利原则

> **"折价不申购，溢价不赎回"**

> **"上涨赚净值，下跌赚溢价"**

> **"不要怂，就是干！"** （但请计算好成本）

---

*本文档随项目持续更新*
